// boltapi/boltapi.h — umbrella header.
//
// Bolt API: a high-performance C++ HTTP framework forked from FasterAPI's
// coroutine core and built on the Bolt compute library. As milestones land,
// this pulls in app.h / request.h / response.h / router.h / middleware.h.
#pragma once

#include "boltapi/version.h"
// M1+: #include "boltapi/app.h"
